{
  "devDependencies": {
    "@faker-js/faker": "^8.2.0",
    "cypress": "^13.5.0",
    "cypress-cucumber-preprocessor": "^4.3.1"
  },
  "scripts": {
    "cypress:headless": "cypress run",
    "cypress:open": "cypress open",
    "cypress:headless:m": "cypress run --config-file mobile.config.js",
    "cypress:open:m": "cypress open --config-file mobile.config.js"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": false,
    "step_definitions": "./cypress/e2e/specs"
  }
}
